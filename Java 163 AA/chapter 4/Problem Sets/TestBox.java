// testBox 
public class TestBox {
	public static void main(String[] args) {
		// create a line and pass value of length:
		Box line = new Box(10);

		// Create a Rectangle and pass value for length, width:
		Box rectangle = new Box(11, 10);

		// Create a box and pass value for length, width, height:
		Box newBox = new Box(12, 2, 40);
	}
}