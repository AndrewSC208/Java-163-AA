// create purchase application:


import java.util.Scanner;
public class CreatePurchase {
	public static void main(String[] args) {

		// variable declarations:
		int invoiceNum;
		double saleAmt;
		// Keyboard scanner:
		Scanner input = new Scanner(System.in);

		// prompt user for info
		System.out.println("What is the invoice number?(Make sure that it is between 1000 and 8000) >>");
		invoiceNum = input.nextInt();
		// validate invoice number is between 1000 and 8000:
		System.out.println(invoicNum);

		// Make sure that the sale amt in a non negative number:



		// Display all of the info:



	}
}